{
  "operation": "oauth.prm_metadata",
  "method": "GET",
  "path_template": "/.well-known/oauth-protected-resource/mcp",
  "auth": "none",
  "request": {},
  "responses": [
    {
      "status": 200,
      "shape": {
        "resource": "string",
        "authorization_servers": ["string"]
      },
      "example": {
        "resource": "https://app.defernowork.com/mcp",
        "authorization_servers": ["https://app.defernowork.com/mcp"],
        "bearer_methods_supported": ["header"]
      }
    }
  ],
  "client_method": null,
  "client_args_from_example": [],
  "mcp_tool": null,
  "mcp_tool_args_from_example": [],
  "notes": "RFC 9728 — Protected Resource Metadata. Path is suffixed with the resource's path component per RFC 9728 §3.1, so a resource at https://host/mcp publishes its PRM at /.well-known/oauth-protected-resource/mcp."
}
