{
  "operation": "chores.occurrences_set_status",
  "method": "PUT",
  "path_template": "/chores/{id}/occurrences/{date}",
  "auth": "bearer",
  "request": {
    "headers_required": ["Authorization"],
    "body": {
      "required": ["status"],
      "optional": [],
      "example": {"status": "done"}
    }
  },
  "responses": [
    {
      "status": 200,
      "shape": {
        "date": "string",
        "status": "string",
        "_required": ["date", "status"]
      },
      "example": {"date": "2026-05-01", "status": "DoneOnTime"}
    }
  ],
  "client_method": "set_chore_occurrence_status",
  "client_args_from_example": ["status"],
  "mcp_tool": "set_chore_occurrence_status",
  "mcp_tool_args_from_example": ["status"]
}
